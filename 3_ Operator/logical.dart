// লজিক্যাল অপারেটরস (Logical Operators)
// এই অপারেটরগুলো শর্তের মধ্যে লজিক্যাল সিদ্ধান্ত নিতে ব্যবহৃত হয়।

void main(){
  bool a = true;
  bool b = false;
  print(a && b);  // false
  print(a || b);  // true
  print(!a);      // false
}